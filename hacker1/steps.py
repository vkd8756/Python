#python,3
def steps(string,num):
    b=""
    j=len(a)
    if c>j:
        d="%"*j
        return d
    d=a[:j-c]
    for i in range(j-c,j):
        d+="%"
    return d
a="python"
c=8
print(steps(a,c))
